Here’s a single prompt to send to the Replit agent. It will:
	•	Give the table a little “flavor” (stripes, hover states, rounded cards)
	•	Make it truly mobile-first (stacked cards on small screens, classic table on larger)
	•	Add a row-highlight feature for any spec with a "highlight": true flag

Please update our SpecTable component and its usage in one PR:

1. **Enhance the styling (“minimal flavor”)**  
   - Wrap the whole table in a white, rounded-lg container with a subtle shadow.  
   - On desktop (≥640px):  
     • Render as a normal `<table>` with a header row.  
     • Apply zebra striping: odd rows bg-gray-50, even rows white.  
     • On hover over any row, bg-gray-100.  
     • If a spec object has `"highlight": true`, give that row a left border `border-l-4 border-blue-600 bg-blue-50`.  
   - On mobile (<640px):  
     • Render each spec as a full-width card:  
       - Card bg white, rounded-lg, shadow, p-4, mb-4  
       - Parameter label in bold above, value below  
       - If `"highlight": true`, card gets `border-l-4 border-blue-600 bg-blue-50`.  
   - Use Tailwind classes only.

2. **Adjust the component code**  
   ```tsx
   // components/SpecTable.tsx
   interface Spec { Model: string; Size_in: string; /* …other fields… */ Lubrication: string; highlight?: boolean; }
   export default function SpecTable({ specs }: { specs: Spec[] }) {
     return (
       <div className="bg-white rounded-lg shadow overflow-hidden">
         {/* Desktop */}
         <table className="hidden sm:table w-full">
           <thead className="bg-blue-600 text-white">
             <tr>
               <th className="px-4 py-2 text-left">Model</th>
               <th className="px-4 py-2 text-left">Size (in)</th>
               {/* …other headers… */}
               <th className="px-4 py-2 text-left">Lubrication</th>
             </tr>
           </thead>
           <tbody>
             {specs.map((s, i) => (
               <tr
                 key={s.Model}
                 className={`
                   ${i % 2 ? 'bg-gray-50' : 'bg-white'}
                   hover:bg-gray-100
                   ${s.highlight ? 'border-l-4 border-blue-600 bg-blue-50' : ''}
                 `}
               >
                 <td className="px-4 py-2">{s.Model}</td>
                 <td className="px-4 py-2">{s.Size_in}</td>
                 {/* …other cells… */}
                 <td className="px-4 py-2">{s.Lubrication}</td>
               </tr>
             ))}
           </tbody>
         </table>
         {/* Mobile */}
         <div className="sm:hidden p-4">
           {specs.map(s => (
             <div
               key={s.Model}
               className={`
                 bg-white rounded-lg shadow mb-4 p-4
                 ${s.highlight ? 'border-l-4 border-blue-600 bg-blue-50' : ''}
               `}
             >
               <div className="font-bold mb-1">Model</div>
               <div className="mb-2">{s.Model}</div>
               <div className="font-bold mb-1">Size (in)</div>
               <div className="mb-2">{s.Size_in}</div>
               {/* …repeat for each field… */}
               <div className="font-bold mb-1">Lubrication</div>
               <div>{s.Lubrication}</div>
             </div>
           ))}
         </div>
       </div>
     );
   }

	3.	Usage

// pages/products/single-toggle-jaw-crusher.tsx
import specs from '../../data/specs/single-toggle-jaw-crusher.json';
import SpecTable from '../../components/SpecTable';

export default function SingleToggle() {
  return (
    <>
      {/* …other content… */}
      <SpecTable specs={specs} />
      {/* … */}
    </>
  );
}


	4.	Clean-up
	•	Remove any leftover CSS or old table markup.
	•	Ensure no horizontal scroll on mobile; cards stack.
	•	Verify that if you add "highlight": true to any entry, that row/card shows the blue accent.

This will give you a more polished table, true mobile stacking, and a clear “highlight” indicator whenever you set `highlight: true` on a spec entry.