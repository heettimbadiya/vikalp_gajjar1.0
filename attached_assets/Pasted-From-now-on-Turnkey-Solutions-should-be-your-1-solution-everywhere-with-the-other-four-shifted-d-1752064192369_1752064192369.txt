From now on “Turnkey Solutions” should be your #1 solution everywhere, with the other four shifted down. Please implement these changes in one go:

1. **Data source**  
   - Edit `/data/solutions.json` so Turnkey is first:
     ```json
     [
       { "name": "Turnkey Solutions",      "slug": "turnkey-solutions" },
       { "name": "Mining & Aggregates",    "slug": "construction" },
       { "name": "Sand Washing & M-Sand",  "slug": "sand-washing-m-sand" },
       { "name": "Mobile & Modular Plants","slug": "mobile-modular-plants" },
       { "name": "C&D Recycling",         "slug": "c-and-d-recycling" }
     ]
     ```

2. **Front-end hero**  
   - In your Home (or Solutions) page loader (`getStaticProps` or server loader), import the updated JSON and pass it into `<SolutionsHero>`.  
   - Replace any hard-coded pills with:
     ```tsx
     {solutions.map(sol => (
       <Link key={sol.slug} href={`/solutions/${sol.slug}`}>
         <a className="snap-center …">{sol.name}</a>
       </Link>
     ))}
     ```

3. **Turnkey page scaffold**  
   - Create `pages/solutions/turnkey-solutions.tsx` (or ensure your `[slug].tsx` handles “turnkey-solutions”).  
   - Copy the pattern from the other four detail pages (hero, grid, FAQ, industry sections) and customize titles/text as “Turnkey Solutions.”  
   - Place its media under:
     ```
     public/media/solutions/turnkey/intro.mp4
     public/media/solutions/turnkey/intro-poster.jpg
     ```

4. **Backend cleanup**  
   - Remove any legacy `/pages/api/solutions/*` or old JSON files so only `/data/solutions.json` remains.  
   - Confirm no stale imports reference the old order or endpoints.

5. **Media streaming**  
   - In `<VideoHero>` for home/about, set `controls={false}`, `autoplay`, `loop`, `playsInline`, and point `src` to `/media/home/intro.mp4` or `/media/about/overview.mp4`.  
   - Ensure your server supports byte-range requests on `/public/media/**` so videos start instantly.

6. **Rebuild & test**  
   - Restart your dev server.  
   - Verify on mobile that hero pills wrap into rows and “Turnkey Solutions” appears first.  
   - Navigate to `/solutions/turnkey-solutions` and confirm the new page renders like the other four.

Implement all of the above in one commit so we end up with:
- A single, ordered JSON backend  
- A dynamic, data-driven SolutionsHero with Turnkey first  
- A fully scaffolded Turnkey Solutions page  
- No dead code or extra endpoints  
- Fast-loading, control-free videos  